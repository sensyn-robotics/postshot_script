# COLMAP レジストレーションテスト結果

**日付:** 2026年2月4日〜2026年2月5日
**シーン:** シーン1（①腕金を両サイド1本ずつ）

## 問題の概要

COLMAPは全254枚の画像のうち、わずか**2枚のみ**（W画像2枚、Z画像0枚）しかレジストレーションできませんでした。
これにより、ズームカメラ（Z）の画像が使用されず、3DGS結果がぼやけてしまいます。

## 結果サマリー（改善設定での全データセット）

### 改善前（オリジナル設定）
- レジストレーション済み: 2枚（W 2枚、Z 0枚）
- レジストレーション率: 0.8%

### 改善後（緩和設定）
| 再構成 | W画像 | Z画像 | 合計 | 3Dポイント数 |
|--------|-------|-------|------|--------------|
| 0（メイン） | 127 | 5 | 132 | 58,302 |
| 1 | 19 | 0 | 19 | 9,711 |
| 2 | 0 | 11 | 11 | 1,808 |
| **合計** | **127** | **16** | **162** | - |

**改善結果:** メイン再構成で2枚から132枚に増加
- **全127枚のW画像が正常にレジストレーション完了**
- **5枚のZ画像がメイン再構成でW画像と共にレジストレーション**
- **追加の11枚のZ画像が別の再構成を形成**

**残存課題:** 再構成2の11枚のZ画像は、W優勢の再構成との間にクロスカメラマッチが不足しているため、マージできません。

## 原因分析

### マッチング統計（全データセット: 254枚）
- **W-Wマッチ:** 4591ペア、平均937.7マッチ/ペア
- **Z-Zマッチ:** 1681ペア、平均581.6マッチ/ペア
- **W-Zマッチ:** 3959ペア、平均71.7マッチ/ペア

**重要な発見:** クロスカメラ（W-Z）マッチは存在するが、同一カメラマッチより**13倍弱い**。
これにより、COLMAPはカメラごとに別々のモデルを構築し、統合できなくなります。

### W-Zマッチングが弱い理由
1. **異なる焦点距離** - W（ワイド）とZ（ズーム）カメラは大きく異なるFOVを持つ
2. **スケールの違い** - 同じ物体がWとZで異なるサイズで写る
3. **限られたオーバーラップ領域** - W画像の中央部分のみがZ画像とオーバーラップ

## 改善された設定（config_improved_registration.json）

```json
{
  "colmap": {
    "feature_extractor": {
      "ImageReader.single_camera_per_folder": 1,
      "ImageReader.camera_model": "OPENCV",
      "SiftExtraction.max_num_features": 8192
    },
    "matcher": {
      "type": "exhaustive"
    },
    "mapper": {
      "Mapper.init_min_num_inliers": 15,
      "Mapper.abs_pose_min_num_inliers": 5,
      "Mapper.abs_pose_min_inlier_ratio": 0.05,
      "Mapper.init_max_error": 8,
      "Mapper.min_model_size": 10
    }
  }
}
```

## テスト結果サマリー（小規模データセット: 30枚 = W 15枚 + Z 15枚）

| テスト | 合計 | レジストレーション | W | Z | 時間(秒) |
|--------|------|-------------------|---|---|----------|
| baseline | 30 | 16 | 10 | 6 | ~21 |
| very_relaxed | 30 | 18 | 11 | 7 | ~27 |
| simple_radial | 30 | 16 | 10 | 6 | ~19 |
| cross_camera_focus | 30 | 18* | 11 | 7 | ~25 |
| single_camera_all | 30 | 17** | 11 | 6 | ~55 |

*cross_camera_focusは5つの再構成を作成。メインは18枚
**single_camera_allは複数カメラで再構成が断片化

## 作成したスクリプト

| スクリプト | 目的 |
|------------|------|
| `scripts/create_small_dataset.ps1` | 30枚テストセット作成（W 15枚 + Z 15枚） |
| `scripts/test_colmap_registration.ps1` | 設定変更可能なCOLMAPテスト実行 |
| `scripts/run_single_test.ps1` | 事前定義設定でのクイックテスト |
| `scripts/test_full_dataset.ps1` | 全データセットで改善設定をテスト |
| `scripts/read_colmap_model.py` | COLMAP再構成の分析 |
| `scripts/analyze_matches.py` | マッチング統計の分析 |
| `scripts/resize_images.py` | 解像度テスト用の画像リサイズ |
| `scripts/merge_reconstructions.ps1` | 複数COLMAPモデルのマージ |
| `config/config_improved_registration.json` | デュアルカメラ用改善設定 |
| `config/config_test_relaxed.json` | 緩和設定テスト用 |

## 出力場所

- **小規模データセット:** `output_test_small/`
- **改善版フルラン:** `output_improved_registration/`
  - `sparse/0/` - メイン再構成（132枚: W 127枚 + Z 5枚）
  - `sparse/1/` - セカンダリW再構成（19枚）
  - `sparse/2/` - Zのみの再構成（11枚）

## 推奨事項

### 即時対応
1. **改善された設定を使用**（`config/config_improved_registration.json`）
2. **メイン再構成（sparse/0）を使用** - Z画像5枚を含む132枚

### 将来の改善案
1. **画像リサイズ** - WとZのスケール差を軽減するため縮小
2. **カスタムペアマッチング** - 同一タイムスタンプのW-Zペアを優先
3. **マルチカメラリグ制約** - W-Z間の固定ポーズ関係を追加

## 結論

レジストレーション診断により、弱いW-Z特徴マッチがクロスカメラレジストレーションを妨げていることが判明しました。
COLMAPの設定を緩和することで、レジストレーションはメイン再構成において2枚から132枚に改善し、
127枚のW画像に加えて5枚のZ画像も正常にレジストレーションされました。

全254枚が単一の再構成に含まれていませんが、メイン再構成には全てのW画像と一部のZ画像が含まれており、
元のW画像のみの結果と比較して、3DGS品質が大幅に向上するはずです。

# Python dependencies for postshot_script
# Managed by uv: use "uv sync" to install from pyproject.toml
# This file is kept for reference / pip fallback only.

# Core dependencies
numpy>=1.20.0
pillow>=9.0.0

# Computer vision for blur detection and optical flow
opencv-python>=4.5.0

# Matplotlib for visualization (optional, for point cloud rendering)
matplotlib>=3.5.0

# 3DGS quality evaluation (LPIPS)
torch>=2.5.0
torchvision>=0.20.0
gsplat>=1.5.0
lpips>=0.1.4
plyfile>=1.1.0
